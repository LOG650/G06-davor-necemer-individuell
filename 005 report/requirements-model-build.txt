# LOG650 report model/build environment
#
# Purpose:
# - Document the Python package versions used for the published model run.
# - Keep the report rebuild command reproducible enough for final QA.
#
# Model run command:
# uv run --python 3.12 --with pandas==3.0.2 --with scipy==1.17.1 --with statsmodels==0.14.6 python "005 report/scripts/run_forecast_capacity_models.py"
#
# Logged in 004 data/processed/model_run_summary.json:
pandas==3.0.2
scipy==1.17.1
statsmodels==0.14.6

# Figure rebuild command used during report work:
# uv run --python 3.12 --with pandas --with numpy --with matplotlib python "005 report/scripts/build_report_figures.py"
#
# PDF rebuild:
# python "005 report/scripts/build_report_pdf_latex.py"
# Requires Pandoc 3.9.x and TinyTeX/xelatex with Norwegian hyphenation.
